import React from "react";

// Confetti button removed — keep a no-op stub to avoid breaking imports
export const ConfettiButton: React.FC<{ onClick?: () => void }> = () => null;

ConfettiButton.displayName = "ConfettiButton";
